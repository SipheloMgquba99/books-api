name: .NET Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '8.0.x'  # or your specific version

    - name: Restore dependencies
      run: dotnet restore Library-System.sln  # Specify your solution file here

    - name: Build the project
      run: dotnet build Library-System.sln --configuration Release --no-restore

    - name: Run tests
      run: dotnet test Library-Systen-Tests/Library-Systen-Tests.csproj --no-restore --verbosity normal  # Specify test project
